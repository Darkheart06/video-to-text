# Расшифровка записей

Приложение для macOS: берёт видео- или аудиозапись и отдаёт транскрипт с
таймкодами и разделением по спикерам, краткое саммари, бриф, список задач и
принятых решений.

**Всё считается на вашем компьютере.** Ни файл, ни текст никуда не отправляются:
распознавание — Whisper, спикеры — sherpa-onnx, саммари — языковая модель,
которую вы подключаете сами. Интернет нужен один раз, чтобы скачать модели.

🇬🇧 [In English](README.md)

![Окно приложения: идёт запись созвона, слева архив, ниже разобранная встреча](docs/screenshots/main.ru.png)

Форматы: `mp4`, `mov`, `webm`, `mkv`, `avi`, `m4v` · `mp3`, `m4a`, `wav`, `flac`,
`ogg`, `opus`, `aac`.

---

## Установка

```bash
git clone https://github.com/Darkheart06/video-to-text.git
cd video-to-text
bash install.sh
```

Скрипт идёт по шагам и спрашивает перед каждой большой загрузкой: проверит
Python и ffmpeg, создаст окружение `.venv`, поставит библиотеки, скачает модели
для разделения по спикерам, поможет с языковой моделью и соберёт
`Расшифровка.app`.

Что нужно иметь заранее (скрипт подскажет, если чего-то нет):

| Компонент | Зачем | Как поставить |
|---|---|---|
| Python 3.9+ | всё приложение | обычно уже есть; иначе `brew install python@3.12` |
| ffmpeg | достать звук из видео | `brew install ffmpeg` |
| Языковая модель | саммари и бриф | см. следующий раздел |

Без языковой модели приложение работает — просто не делает саммари.

## Подключение языковой модели

Способа три, переключаются в «Настройках» → «Как подключена модель». Кнопка
«Проверить связь» там же реально дёргает модель и показывает её ответ.

### 1. Ollama — если не хочется разбираться

```bash
brew install --cask ollama
ollama pull gemma4:12b        # или gemma4:12b-mlx, см. ниже
```

Дальше ничего настраивать не нужно: приложение само находит Ollama на
`127.0.0.1:11434` и берёт лучшую из установленных моделей. В настройках можно
выбрать конкретную.

### 2. Файл .gguf на диске — если модель уже скачана

Скачиваете `.gguf` (например с Hugging Face), в настройках выбираете «Файл
.gguf на диске» и указываете путь. Никакого сервера запускать не надо.

Для этого нужна библиотека, которая умеет читать `.gguf` — `install.sh`
предлагает её поставить, но можно и вручную:

```bash
CMAKE_ARGS="-DGGML_METAL=on" .venv/bin/pip install llama-cpp-python
```

Собирается из исходников 3–10 минут и требует инструментов разработчика
(`xcode-select --install`). Флаг `-DGGML_METAL=on` включает счёт на видеоядре —
без него будет в разы медленнее.

Какой файл брать: ищите `Q4_K_M` в имени — это разумный компромисс между
качеством и размером. `Q5_K_M` чуть лучше и на четверть больше, `Q8_0` — почти
без потерь, но вдвое тяжелее.

### 3. Свой сервер с OpenAI API — если модель уже где-то крутится

Подходит LM Studio, `llama-server` из llama.cpp, Jan, LocalAI и любой другой
сервер с `/v1/chat/completions`. В настройках укажите адрес (у LM Studio это
`http://127.0.0.1:1234/v1`, у llama-server — `http://127.0.0.1:8080/v1`),
модель и, если нужен, ключ.

Тот же путь работает и с облачным сервисом, если вы сознательно хотите
отправлять расшифровку наружу — тогда впишите его адрес и ключ.

### Какую модель выбрать под свою память

Размер модели должен помещаться в оперативную память с запасом гигабайта в
три-четыре на систему и само приложение.

| Память Mac | Разумный выбор | Размер |
|---|---|---|
| 16 ГБ | `gemma4:e4b` | ~10 ГБ |
| 24 ГБ | `gemma4:12b` | ~8 ГБ |
| 32–48 ГБ | `gemma4:12b`, с запасом `gemma4:26b` | 8 / 19 ГБ |
| 64 ГБ и больше | `gemma4:26b` или `gemma4:31b` | 19 / 20 ГБ |

На Apple Silicon у моделей в Ollama есть варианты с суффиксом `-mlx`
(`gemma4:12b-mlx`) — то же самое, но собранное под Metal, работает заметно
быстрее. `install.sh` пробует сначала их.

**Считайте не размер файла, а занятую память.** С контекстом 32K
`gemma4:12b-mlx` занимает **16 ГБ**, хотя весит 7.7 ГБ: остальное — кэш
контекста. На маке с 24 ГБ это уже впритык, а модель на 27B туда не помещается
вовсе, что бы ни писали обзоры про видеокарты на 24 ГБ.

**Замер на настоящем созвоне** (51 минута, 40 тысяч знаков расшифровки,
M4 Pro / 24 ГБ, `tools/modeltest.py`):

| Модель | Память | Время | Что получилось |
|---|---|---|---|
| `gemma4:12b-mlx` | 16 ГБ | 186 с | ровное саммари, но сроки в задачах почти везде «—» |
| `qwen3.5:9b-mlx` | 9.3 ГБ | 182 с | на четверть подробнее, вытащил сроки («завтра», «до завтра») и условия |

Вывод для 24 ГБ: `qwen3.5:9b-mlx` — та же скорость, больше конкретики и на
семь гигабайт меньше памяти. Проверить на своей записи:

```bash
python tools/modeltest.py "~/Documents/Расшифровка записей/Созвон.result.json" \
    gemma4:12b-mlx qwen3.5:9b-mlx
```

## Сборка установщика (DMG)

Чтобы отдать приложение человеку, у которого нет ни Python, ни Homebrew:

```bash
bash packaging/make-dmg.sh
```

В папке `dist/` появится образ `Расшифровка-записей-1.10.2.dmg` — пара мегабайт.
Внутри «Установить Расшифровку.app»: он сам скачивает Python, ffmpeg и модели,
собирает приложение и кладёт его в «Программы». Терминал получателю не нужен.

Образ подписан самоподписанным сертификатом, поэтому на чужом Mac установщик
надо открыть **правой кнопкой → «Открыть»** — один раз. Это написано и в записке
внутри образа. Чтобы обойтись без этого, нужна платная подписка Apple Developer
и нотаризация.

| Файл | Отвечает за |
|---|---|
| `packaging/make-dmg.sh` | собирает образ целиком |
| `packaging/installer.applescript` | окно установщика с полосой прогресса |
| `packaging/install-payload.sh` | что именно происходит при установке |
| `packaging/dmg-readme.txt` | записка, которую видно при открытии образа |

Установленная копия живёт в `~/Library/Application Support/VideoToText`,
результаты — в `~/Documents/Расшифровка записей`, журнал установки — в
`~/Library/Logs/Расшифровка-установка.log`.

## Два языка

Интерфейс и документы говорят по-русски или по-английски, и это две разные
настройки. Язык окна — про того, кто нажимает кнопки. Язык документов — про
того, кто будет читать бриф: от него зависит, на каком языке пишет модель, как
называется папка с результатами («Расшифровка записей» или `Transcripts`) и что
стоит в строке итога — «Итого» или «Total».

При первом запуске оба берутся из системы: на русском маке окно русское, на
любом другом — английское. Переключаются в «Настройках» → «Язык», окно
перерисовывается сразу, без перезапуска. Архив читает обе папки, так что при
переключении ничего не теряется.

Язык записи — вещь отдельная: настройка «Язык» относится к распознаванию, и
Whisper понимает далеко не только эти два.

## Как пользоваться

Двойной клик по `Расшифровка.app` → выберите вверху, **что готовить из записи**,
и перетащите файл в окно (или нажмите «Выбрать запись»). Дальше видно прогресс по
этапам, а в конце — вкладки с разделами выбранного профиля и «Транскрипт».

Спикеров можно переименовать прямо в окне: впишите имена в поля внизу и нажмите
«Применить имена» — файлы транскрипта перезапишутся с новыми подписями.

### Что готовить из записи

Один и тот же транскрипт нужен для разного, поэтому наверху окна есть
переключатель профиля. Он же — в настройках и в ключе `--preset`.

| Профиль | Что получится |
|---|---|
| **Встреча или созвон** | саммари, бриф, решения, задачи, риски |
| **Смета по надиктованному** | таблица работ со ставками, количеством и итогом |
| **Голосовая заметка** | мысли по темам, задачи, названные цифры и списки |
| **Интервью с пользователем** | боли, дословные цитаты, выводы |
| **Свои правила** | всё, что опишете словами |

**Про смету.** Надиктуйте как есть: «демонтаж перегородки — двенадцать часов по
три тысячи, штробление — восемь часов по две с половиной». Модель разложит это по
колонкам, но умножать и складывать будет не она, а приложение: языковые модели
уверенно ошибаются в арифметике, и заметить это в готовом документе почти
невозможно. Рядом с `.summary.md` появится `.таблицы.csv` — открывается в Numbers
и Excel, числа в нём без валюты и пробелов, так что суммы можно считать дальше.

**Свои правила.** В настройках, раздел «Что готовить из записи»: в одном поле
пишете обычными словами, что нужно и чего делать нельзя, в другом — шаблон
документа. Каждый заголовок `##` в шаблоне становится вкладкой в окне и разделом
в файле. Колонки с названиями вроде «Количество» и «Ставка» приложение
перемножит само и допишет строку «Итого» — в любом профиле, не только в смете.

![Профиль «Смета»: таблица работ с посчитанным итогом](docs/screenshots/estimate.ru.png)

### Кто говорит: голоса прямо во время записи

Голоса различаются **по ходу разговора**, а не после него: каждая реплика,
которой хватает на голосовой отпечаток, сравнивается с уже услышанными и сразу
получает номер — «Спикер 1», «Собеседник 2». Строка голосов над транскриптом
показывает их все, а клик по голосу даёт ему имя: одно имя вместо десятка
отметок, и подпись меняется у всех реплик этого человека — и сказанных раньше,
и тех, что он скажет дальше.

Порог подобран так, чтобы ошибаться в сторону лишнего голоса, а не слипшихся.
Два чипа с одним именем сводятся одним кликом, а двух склеенных людей уже не
разделить. Замер на публичном примере с четырьмя голосами и репликами по 2–5
секунд: при пороге 0.50 нашлось два голоса и разные люди слиплись 22 раза, при
рабочем 0.62 — пять голосов и четыре слипания. После остановки запись всё равно
разбирается целиком, и это точнее всего, что возможно на лету.

Один человек не должен разъезжаться на трёх «спикеров», и против этого работают
три правила: голосу, который говорил только что, даётся фора (в середине фразы
люди не меняются); голоса, оказавшиеся одним человеком, сводятся по ходу
разговора; а если вы перечислили, кто на связи, приложение не заводит голосов
больше, чем людей (плюс один про запас). И самое простое: два чипа с одним
именем — это один человек, они сливаются в один голос.

Имена можно ставить и руками: впишите их в поле «+ имя» и дальше одним кликом
отмечайте, кто говорит, — либо нажмите имя (отметится последняя реплика), либо
кликните саму реплику и выберите человека. Цифры 1–9 делают то же самое с
клавиатуры, не отрывая рук.

Отметить достаточно по одной реплике на человека. Когда запись остановится,
приложение сравнит отмеченные куски речи с остальными и подпишет именами всю
запись: в транскрипте будут «Ирина» и «Дмитрий», а не «Спикер 2» и «Спикер 3».

Сравнение идёт по каждой реплике отдельно, а не по усреднённому голосу
кластера, и решает не абсолютная похожесть, а разрыв между лучшим и вторым:
на настоящей записи кусок речи и средний отпечаток своего же кластера сошлись
всего на 0.52, тогда как чужой голос дал 0.40. Если разрыва нет — имя не
ставится: «Спикер 2» лучше, чем чужое имя в чужих словах.

### Знакомые голоса

Разделение по голосам работает внутри одной записи: видно, кто с кем совпал, но
не кто это. Имя приходится ставить заново каждый раз — а созваниваетесь вы
обычно с одними и теми же людьми.

Поэтому есть память между записями. Откройте разобранную запись, проверьте, что
имена стоят верно, и нажмите **«Запомнить голоса»** — и дальше эти люди
узнаются сами: имя появляется в живом транскрипте, как только человек заговорил,
и в готовых файлах и саммари без единого клика.

**Запоминание запускается только по команде** — и это осознанно. Одна ошибка
разделения, запомненная молча, осталась бы навсегда: приложение выучило бы, что
Леонид звучит как его собеседница, и подписывало бы так все следующие созвоны.
Поэтому сначала вы смотрите на имена, потом нажимаете кнопку.

Запоминаются только настоящие имена: «Спикер 2» и «Собеседник 1» ничего не
значат и пропускаются. Одно имя достаётся одному голосу в записи — если на
Леонида похожи двое, имя получает тот, кто похож сильнее, а второй остаётся с
номером. Кого приложение знает, видно в настройках, там же любой голос забывается
одной кнопкой, а узнавание целиком выключается галочкой.

Дообучения нейросети здесь нет и не нужно: модель не меняется, рядом с ней
хранится несколько векторов на человека — те же самые, что приложение и так
сравнивает внутри записи. Дообучение потребовало бы часов речи каждого человека
и видеокарты, а помогло бы там, где мы и так не ошибаемся.

То же самое из консоли:

```bash
./run.sh --learn "~/Documents/Расшифровка записей/Созвон 26.08.result.json"
./run.sh --voices          # кого приложение узнаёт
./run.sh --forget Леонид   # забыть голос
```

### Запись встречи в комнате

Кнопка «Записать встречу» рядом с записью созвона. Пишется только микрофон,
разрешение на запись экрана не нужно — на совещании за столом все голоса и так
идут в один микрофон. После остановки запись разбирается по голосам так же, как
обычный файл, а отмеченные по ходу имена подставляются по всей записи.

### Запись созвона

Кнопка «Начать запись созвона» — или приложение само предложит, когда заметит,
что микрофон занят. macOS попросит разрешение на запись экрана: именно оно даёт
доступ к системному звуку. Кнопка в приложении открывает нужный раздел настроек
сразу, руками искать не надо.

Свой микрофон и системный звук пишутся раздельно, поэтому «я» и «собеседник» не
путаются между собой.

### Кто говорит на созвоне

Во время разговора приложение различает только «Я» и «Собеседник»: дорожки дают
ровно это — мой микрофон и всё остальное. Когда запись останавливается,
системная дорожка отдельно разбирается по голосам, и в готовом транскрипте
появляются «Собеседник 1», «Собеседник 2» и так далее. Своя дорожка в разбор не
идёт — мой голос там и так известен, и он только мешал бы кластеризации.

Занимает это около двух минут на час разговора. Отключается галочкой
«Различать собеседников по голосам» в настройках.

### Правка саммари

Модель иногда тащит в бриф то, что к разговору не относится, или задачу, которой
никто не ставил. Кнопка **«Править»** справа над текстом включает точечную
правку: крестик убирает пункт или строку таблицы, по самому тексту можно кликнуть
и переписать. «Сохранить» перезаписывает саммари и таблицы, а суммы в сметах
пересчитываются заново.

**Транскрипт при этом не меняется.** Сказанное остаётся сказанным — правится
только разобранный документ.

### Названия записей

Записи называются по теме разговора, а не «Созвон 2026-08-27 13-32». Название
придумывает та же модель, что делает саммари, и ставится перед датой:
«Логика геймификации 2026-08-27 13-32». Дата остаётся, чтобы записи по-прежнему
выстраивались по времени. Если модель не ответила — остаётся прежнее имя.

### Сроки становятся датами

В разговоре срок называют словами: «до завтра», «к концу месяца», «в пятницу».
Через неделю в списке задач это уже ничего не значит, поэтому дата записи
известна — и приложение дописывает рядом, что имелось в виду: «завтра
(28 августа)». Сказанные слова остаются на месте.

Считает арифметику код, а не модель: с датами языковые модели ошибаются так же
уверенно, как с умножением. И только в колонках со сроком — в связном тексте
«до пятницы» может оказаться цитатой, а не обещанием.

### Сколько получается голосов

После разделения приложение сверяет голоса между собой и сводит те кластеры,
которые оказались одним человеком. Вопрос в том, что считать «одним и тем же», и
число в настройках на него не отвечает: насколько похожи два куска речи одного
человека, зависит от микрофона, комнаты и связи — на одной записи это 0.95, на
другой 0.70.

Поэтому точку отсчёта берём из самой записи. Речь каждого голоса режется
пополам, половины сравниваются между собой, и получается «насколько человек
похож сам на себя вот здесь». Заодно это мера доверия к сравнению вообще: 0.95 —
отпечатки устойчивые, порог можно поднять почти к этому числу; 0.70 — реплики
короткие или микрофон плохой, и разные люди легко наберут 0.8, сводить голоса
опасно. **Поэтому порог только растёт** и никогда не опускается ниже
`speaker_merge_similarity`: хуже, чем было, не станет, а на хорошей записи станет
аккуратнее.

Половины набираются вперемешку, а не «первая половина встречи и вторая»: если в
кластер попали двое, оба окажутся в обеих половинах, и ошибка уйдёт в безопасную
сторону — порог станет строже, а не мягче.

Отключается ключом `speaker_merge_auto` в `config.json` или галочкой в
настройках. А если число участников известно — надёжнее всего просто указать его
в «Сколько спикеров»: тогда ничего подбирать не нужно.

Проверить на своих записях, с правильным ответом или без:

```bash
python tools/speakertest.py "~/Documents/Расшифровка записей/Созвон.wav" --было 4
python tools/speakertest.py "~/Documents/Расшифровка записей" --список правда.txt
```

### Запись экрана и метки

![Выбор того, что писать с экрана: плитки со снимками экрана и окон приложений](docs/screenshots/screen-pick.ru.png)

На созвонах показывают экран: макет, таблицу, ошибку в интерфейсе. Рядом с
кнопкой записи выбирается, что писать: **только звук**, **весь экран** или
**одно приложение** из запущенных. Картинка идёт тем же потоком
ScreenCaptureKit, что и системный звук, — отдельного разрешения и виртуального
драйвера не нужно. Пишется 8 кадров в секунду при ширине 1600 точек: это показ
окна, а не кино, поэтому час занимает сотни мегабайт, а не десятки гигабайт.

**Экран включается и выключается посреди созвона.** Ту же кнопку видно в шапке
идущей записи: показ начался — включили, закончился — выключили, и час разговора
не превращается в час видеозаписи пустого рабочего стола. Звук при этом не
прерывается: помощник захвата тот же, меняется только его настройка.

![Идёт запись созвона, картинка пишется из Safari — кнопка в шапке подсвечена](docs/screenshots/screen-live.ru.png)

Картинка, включённая посреди разговора, сохраняется **отдельным файлом**
`<запись>.screen-1.mp4` со своим куском звука. Главным видео запись становится
только тогда, когда шла от начала и до конца созвона: у такой картинка и звук
считаны от одной точки, и метки в плеере ведут туда, куда обещают.

Готовая запись играет прямо в карточке, а над плеером стоят **метки** — там, где
прозвучало то, что попало в документ: решение, задача, риск, заметка по ходу.
Клик по метке — и запись играет с этого места, как главы у видео на YouTube.

**Время меток берётся из расшифровки, а не у модели.** Спросить модель «во
сколько это сказали» — верный способ получить правдоподобное выдуманное число.
Каждый пункт документа сопоставляется с репликами по словам, и метка получает
время найденной реплики; не нашлось убедительного совпадения — метки нет.
Пустое место честнее, чем метка, ведущая не туда.

![Готовая запись: плеер, метки на шкале и список меток под ним](docs/screenshots/marks.ru.png)

Метки сохраняются и файлом `<запись>.chapters.vtt` — его понимают плееры,
YouTube и монтажные программы.

### Расписание созвонов и напоминания

![Расписание: события из разных календарей одним списком, пересечения помечены](docs/screenshots/agenda.ru.png)

Встречи прилетают в разные календари — рабочий Gmail, личный Яндекс, чужой
Outlook, — и, договариваясь о времени прямо на созвоне, все их не помнишь.
Значок календаря в шапке открывает **один список**, где **пересечения по
времени помечены**: список нужен не чтобы посмотреть «что сегодня», а чтобы не
назначить второй созвон поверх первого. Ближайший созвон видно и без списка —
строкой в рабочей области, с кнопкой «Записать».

События берутся из **системного Календаря macOS**: Gmail, Outlook и Яндекс уже
синхронизируются туда, поэтому одно системное разрешение заменяет три
интеграции, и ради чтения календаря ничего не уходит с машины. Созвон можно
завести и из приложения — по желанию сразу в настоящий календарь, чтобы его
увидели остальные; пока набираешь время, окно подсказывает, что на него уже
стоит.

**Когда напоминать — выбираете вы.** Интервалы отмечаются кнопками: за сутки,
за два часа, за час, за 30, 15, 5 минут, в момент начала — сколько угодно
сразу, и рядом добавляется своё число минут. Одного «за полчаса» не хватает на
все случаи: к чужой планёрке хватит пятиминутного, а к разговору с заказчиком
нужен день на подготовку. У любого созвона в расписании — свой набор:
колокольчик в строке события переопределяет общий, выбранное видно прямо в
строке, а пустой набор возвращает к общему.

![Настройки: интервалы напоминаний кнопками, рядом поле для своего числа минут](docs/screenshots/reminders.ru.png)

Каждый интервал срабатывает один раз и независимо от других. Если приложение
было закрыто и разом наступило несколько отметок, придёт одно напоминание — по
ближайшей к текущему моменту, а не очередь одинаковых. Напоминание показывается
плашкой в окне, а по желанию — системной плашкой macOS и сообщением в Telegram
или MAX; мессенджеры выключены по умолчанию, и в них уходит только время,
название и — отдельными галочками — участники и итоги разбора.

### Документы к записи

Созвон почти никогда не самодостаточен: обсуждают смету, техзадание, письмо
заказчика, а в расшифровке остаётся «как в том документе». Через неделю по
задаче «доработать по замечаниям» уже не понять, о каких замечаниях речь.

Блок **«Документы к записи»** на карточке (свёрнут, раскрывается кликом) даёт
приложить файлы к записи. Их текст уходит в модель вместе с расшифровкой —
поэтому в саммари попадают точные названия, цифры и формулировки из документа.
Кнопка **«Пересобрать саммари с документами»** переписывает только саммари;
расшифровку она не трогает, потому что та не менялась.

Картинки и документы лежат на разных полках: снимки экрана и фотографии — это
квадратные превью, клик по превью открывает картинку во весь экран (Escape
закрывает), а документы остаются списком и открываются в своей программе.
Кнопка **«Редактировать список»** переключает блок из просмотра в удаление —
случайным кликом ничего не денется.

Файлы копируются рядом с записью, в папку `<запись>.files`: исходник
переименуют или удалят, а запись останется цельной. Текст достаётся из `.pdf`,
`.docx`, `.pptx`, `.xlsx`, `.txt`, `.md`, `.csv` и `.rtf`; всё остальное просто
лежит рядом и открывается кликом.

### Справочник людей и команд

Участников созвона приходилось набирать заново каждый раз, хотя созваниваетесь
вы с одними и теми же людьми — и обычно командой. В «Настройках» → **«Люди и
команды»** заводятся люди с указанием компании или команды; там же видно, чей
голос приложение уже запомнило.

Во время созвона рядом с «Кто на связи» появляются команды из справочника: один
клик — и все её участники в списке, поправить дешевле, чем набрать. Отсюда же
работают цифры 1–9 для разметки реплик.

### Одна запись в рабочей области

В середине окна — только та запись, которую вы открыли: кликом в списке слева
или запустив разбор. Раньше карточки копились лентой, и было непонятно, что из
этого свежее и на что вы смотрите. Задания, которые считаются прямо сейчас,
собраны в одну строку «Сейчас в работе» над записью — клик по ней открывает
задание.

### Корзина и как вернуть удалённое

Крестик у записи в архиве стал корзиной, и удаление перестало быть
необратимым: запись целиком — со звуком, транскриптом и саммари — уходит в
корзину приложения. Она видна кнопкой внизу панели: там написано, сколько
записи осталось лежать, кнопка «Вернуть» ставит её на прежнее место, «Стереть»
убирает навсегда, «Очистить» — всё сразу.

Пролежавшее **30 дней** стирается само (`trash_days` в настройках; `0` —
не стирать никогда). Раньше файлы уходили в системную Корзину, и вернуть их
можно было только через Finder по именам файлов — вслепую.

### Разбор не мешает следующему созвону

Разбор записи — разделение голосов и саммари — занимает минуты, и раньше на это
время запись была занята: созвон, начавшийся сразу за предыдущим, записать было
нельзя. Теперь разбор идёт своей очередью: как только начинается новая запись,
он встаёт на паузу, а после — продолжается с того же места. Записи, ждущие
разбора, видно под карточкой записи, так что ничего не выглядит потерянным.

Пауза берётся между этапами, а не посреди них: остановить Whisper на середине
куска нельзя, а между «собрать звук», «разделить голоса» и «саммари» — можно, и
это незаметно.

### Архив записей

Слева — список всего, что уже разобрано: и файлы, и записанные созвоны. Клик по
записи разворачивает её обратно в то же окно с вкладками, что и сразу после
обработки, — искать файлы в папке не нужно. Поиск сверху ищет по названию, а с
трёх букв — и внутри самих расшифровок, так что «где мы обсуждали смету» находится
за секунду. Крестик убирает запись со всеми файлами в Корзину (спрашивает
подтверждение), кнопка «Архив» в шапке прячет панель.

Список строится по файлам `.result.json` в папке результатов, отдельной базы
нет. Поэтому архив переживает переустановку приложения: скопировали папку —
записи снова на месте. Имена спикеров можно поправить и у старой записи —
транскрипт и субтитры перепишутся.

### Копирование текста

Текст расшифровки и саммари выделяется мышью и копируется как обычно — Cmd+C.
Кнопка «Копировать» справа над текстом кладёт в буфер всю открытую вкладку
целиком: раздел саммари как есть или транскрипт с таймкодами. Cmd+A выделяет
текст открытой вкладки, а не всё окно.

Кнопки, вкладки и список слева от выделения защищены — иначе при попытке
выделить абзац «красится» пол-окна.

### Из терминала

```bash
./run.sh встреча.mp4                          # всё по умолчанию
./run.sh *.mov --speakers 3                   # знаем, что говорящих трое
./run.sh лекция.m4a --no-speakers             # один голос, разделение не нужно
./run.sh интервью.mp4 --language ru --model large-v3
./run.sh запись.webm --no-summary             # только транскрипт
./run.sh смета.m4a --preset estimate          # таблица работ с итогом
./run.sh заметка.m4a --preset note            # разложить надиктованное по полочкам

./run.sh --check-llm                          # проверить связь с моделью
./run.sh --learn созвон.result.json           # запомнить голоса из записи
./run.sh --voices                             # кого приложение узнаёт
./run.sh встреча.mp4 --ollama-model gemma4:12b-mlx
./run.sh встреча.mp4 --gguf ~/Models/gemma-4-12b-Q4_K_M.gguf
./run.sh встреча.mp4 --api http://127.0.0.1:1234/v1
```

## Что появляется на выходе

Для файла `встреча.mp4` в папке `~/Documents/Расшифровка записей`:

| Файл | Что внутри |
|---|---|
| `встреча.summary.md` | разделы выбранного профиля: саммари, бриф, задачи… |
| `встреча.таблицы.csv` | таблицы с пересчитанными суммами — если они были |
| `встреча.transcript.md` | транскрипт с таймкодами и спикерами |
| `встреча.transcript.txt` | то же простым текстом |
| `встреча.subtitles.srt` | субтитры |
| `встреча.result.json` | всё вместе для другой обработки и для архива в окне |

### Какая модель лучше именно на ваших записях

Чужие сравнения моделей отвечают на вопрос «что лучше в среднем по интернету».
Нужный вопрос другой — что лучше на *этих* созвонах, с этими микрофонами и
этими людьми: разброс между записями больше разброса между моделями. Поэтому в
приложении есть замер на своём материале.

```
cd <папка проекта>
./bench.sh --new "~/Documents/Расшифровка записей/Созвон 2026-08-27 11-10.wav"
# поправить руками имена и слова в получившемся файле .ref.txt
./bench.sh
```

`bench.sh` берёт питон из окружения приложения — там стоят mlx-whisper и
sherpa-onnx. Системный `python3` их не увидит, а команды `python` на macOS не
существует вовсе.

Печатаются два числа на каждый вариант: **WER** — доля ошибок в словах, и
**WDER** — доля верно распознанных слов, подписанных не тем человеком. Второе и
есть то, что замечают в расшифровке: текст правильный, а реплика приписана
соседу. Достаточно выверить 10–15 минут разговора и обрезать остаток: короткий
проверенный кусок честнее часа непроверенного.

Меняются два узла. **Модель распознавания** — от `large-v3-turbo` (быстрая, по
умолчанию) до `large-v3` и своей, дообученной на нужном языке: в списке моделей
есть пункт «своя», а рядом поле для имени репозитория. **Модель голосов** — ею
и разделяются голоса, и узнаются знакомые; вариантов четыре, все качаются без
токенов и регистраций. Смена модели голосов обнуляет запомненные голоса:
отпечатки, снятые разными моделями, несравнимы, и их придётся запомнить заново.

## Настройки

![Окно настроек: четыре вкладки, на первой — язык, тема и папка результатов](docs/screenshots/settings.ru.png)

Кнопка «Настройки» в окне или файл `config.json` рядом с проектом. Настройки
разложены по вкладкам: **основное**, **разбор**, **голоса** и **ещё** —
чтобы каждый день видеть четыре поля, а не сорок.

**Язык.** Две строки: язык окна и язык документов (`auto` у документов — тот же,
что у окна). От языка документов зависят и имена папки и файлов результата.

**Язык записи.** `auto` определяет сам. Если язык записи известен — укажите его,
будет точнее и быстрее.

**Модель Whisper.** `large-v3-turbo` — разумный выбор по умолчанию. `small` и
`base` заметно быстрее, но ошибаются на именах и терминах.

**Сколько спикеров.** `0` — определить автоматически. Если точно знаете число,
поставьте его: результат ощутимо чище.

**Порог разделения.** Меньше — больше спикеров. Если два голоса склеились в
одного, снизьте до `0.5`; если один человек разбился на несколько — поднимите до
`0.7`. После разделения приложение всё равно сверяет голоса между собой и
склеивает кластеры, которые оказались одним человеком (`speaker_merge_similarity`
в `config.json`, по умолчанию `0.78`; `1.0` — не сверять вовсе). На настоящей
получасовой встрече это убрало 28 «спикеров» до семи. Дальше порог уточняется по
самой записи — см. «[Сколько получается голосов](#сколько-получается-голосов)».

**Что готовить из записи.** Профиль задаёт правила для модели и шаблон
документа — см. раздел выше. «Свои правила» позволяют написать своё, без
программирования.

**Размер контекста.** Сколько текста модель держит за раз. Больше — связнее
итоговый бриф, но нужнее память. 32768 подходит почти всем.

## Сколько это занимает времени

Ориентир для часовой записи на Apple Silicon: распознавание 3–8 минут на
`large-v3-turbo` через MLX, разделение по спикерам 1–3 минуты, саммари 1–4
минуты. На Intel-маках заметно дольше — там имеет смысл модель `medium`.

Первый запуск дольше остальных: модель Whisper (около 1.5 ГБ) скачивается один
раз и потом лежит в `~/.cache/huggingface`.

## Если что-то не работает

**«ffmpeg не найден»** — `brew install ffmpeg`.

**«Ollama не отвечает»** — запустите приложение Ollama (или `ollama serve`).
Проверить: `curl http://127.0.0.1:11434/api/tags`.

**«Сервер модели не отвечает»** — адрес должен заканчиваться на `/v1`, и сервер
должен быть запущен. Проверить: `curl http://127.0.0.1:1234/v1/models`.

**Файл .gguf не открывается** — либо не установлена `llama-cpp-python`, либо
модели не хватает памяти: уменьшите размер контекста или возьмите файл с более
сильным сжатием (`Q4_K_M` вместо `Q8_0`).

**Окно не открывается** — посмотрите ошибку в терминале: `./run.sh` без
аргументов. Всё то же самое доступно консольной командой с файлом.

**Спикеры определились неверно** — задайте точное число спикеров или подвиньте
порог разделения. На записях, где говорят одновременно или звук идёт из одной
дальней точки комнаты, диаризация ошибается чаще.

**Саммари получилось поверхностным** — возьмите модель побольше и выберите её в
настройках.

## Как устроено

```
файл → ffmpeg (моно 16 кГц) → Whisper (текст + таймкоды слов)
                            → sherpa-onnx (кто когда говорил)
                            → сведение по словам → реплики
                            → языковая модель: разбор по частям → итоговый бриф
                            → .md / .csv / .txt / .srt / .json
```

| Файл | Отвечает за |
|---|---|
| `app/media.py` | проверка файла, извлечение звука, нарезка на куски |
| `app/asr.py` | распознавание (MLX или faster-whisper) |
| `app/diarize.py` | разделение по спикерам, сведение голосов одного человека |
| `app/merge.py` | кто произнёс какую фразу, сборка реплик |
| `app/cleanup.py` | чистка от эканья и слов-паразитов |
| `app/llm.py` | три способа подключить языковую модель |
| `app/presets.py` | профили: что именно готовим из записи |
| `app/summarize.py` | промпты, разбор длинных записей по частям |
| `app/compute.py` | пересчёт таблиц: арифметика мимо модели |
| `app/record.py` | запись созвона, две дорожки, заметки по ходу |
| `app/voices.py` | знакомые голоса: память между записями |
| `app/people.py` | справочник людей и команд |
| `app/attach.py` | документы к записи и текст из них |
| `app/marks.py` | метки на записи по расшифровке |
| `app/serve.py` | локальный сервер, из которого играет запись |
| `app/library.py` | архив разобранных записей и корзина |
| `app/render.py` | markdown, csv, srt, json |
| `app/pipeline.py` | порядок этапов, прогресс, отмена |
| `app/main.py` | окно и мост в интерфейс |
| `app/cli.py` | консольная версия |
| `capture/main.swift` | помощник захвата звука на ScreenCaptureKit |
| `tools/selftest.py` | сквозная проверка пайплайна |
| `tools/uicheck.py` | проверка интерфейса в браузере |
| `tools/speakertest.py` | сколько голосов находится в записи |
| `tools/modeltest.py` | сравнение моделей и настроек саммари |
| `tools/recover.py` | сборка прерванной записи из рабочей папки |

Почему именно так — [docs/architecture.ru.md](docs/architecture.ru.md).
Куда двигаться дальше — [docs/roadmap.ru.md](docs/roadmap.ru.md).

## Разработка

```bash
python tools/selftest.py запись.mp4   # весь пайплайн, Whisper и модель — заглушки
python tools/uicheck.py               # интерфейс в headless-браузере
python tools/speakertest.py запись.m4a --было 4   # сколько голосов и мимо ли
python tools/modeltest.py запись.result.json qwen3.5:9b-mlx gemma4:12b-mlx
ruff check .
```

Правки принимаются — [CONTRIBUTING.md](CONTRIBUTING.md).

## Лицензия

[MIT](LICENSE) — пользуйтесь бесплатно, меняйте, распространяйте.

Чужого в приложении две вещи, обе лежат внутри и наружу ни за чем не ходят:

- **Шрифт Onest** — SIL Open Font License, текст в
  [app/ui/fonts/LICENSE-Onest.txt](app/ui/fonts/LICENSE-Onest.txt).
- **Значки Iconsax** (Vuesax), стиль outline — CC BY 4.0, то есть с указанием
  авторства. Подробности и одна неувязка в условиях —
  [app/ui/icons/LICENSE-Iconsax.md](app/ui/icons/LICENSE-Iconsax.md).
