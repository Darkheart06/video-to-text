# Ядро
numpy>=1.24

# Интерфейс — нативное окно macOS (тянет за собой pyobjc)
pywebview>=5.0

# Разделение по спикерам (офлайн, без токенов и регистраций)
sherpa-onnx>=1.10

# Распознавание речи: MLX — быстрый путь для Apple Silicon
mlx-whisper>=0.4 ; sys_platform == "darwin" and platform_machine == "arm64"

# Запасной движок распознавания — работает везде, даёт точный прогресс
faster-whisper>=1.1

# Саммари через Ollama или через сервер с OpenAI API дополнительных
# библиотек не требует — хватает стандартной библиотеки Python.
# Чтобы открывать файл .gguf напрямую, нужна ещё одна (её ставит install.sh
# по запросу, потому что она собирается из исходников несколько минут):
#     CMAKE_ARGS="-DGGML_METAL=on" .venv/bin/pip install llama-cpp-python
